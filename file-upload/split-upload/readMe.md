# 分片上传接口文档

> 分片上传: 利用了三步上传法:
>   1. 预上传, 请求上传会话
>   2. 分片上传, 保存每一个分片
>   3. 完成上传, 分片拼接

## 1. 文件检查

### 1.1 功能描述

检查当前文件是否已经上传过, 如果文件已经上传过直接返回文件信息实现秒传功能

### 1.2 请求说明

> 请求说明: <br>
> 请求方式: GET<br>
> 请求PATH: `/upload/fileCheck/{md5}`

### 1.3 请求参数

字段 | 字段类型  | 字段说明
--- | --- | ---
md5 | string | 文件的 md5 值

### 1.4 返回结果

```json
{

}
```

### 1.5 返回参数

字段 | 字段类型  | 字段说明
--- | --- | ---

## 2. 分片检查

### 2.1 功能描述

检查当前分片是否已经上传过, 如果上传过的话直接跳过当前分片尝试上传下一个分片

### 2.2 请求说明

> 请求说明: <br>
> 请求方式: GET<br>
> 请求PATH: `/upload/chunkCheck`

### 2.3 请求参数

字段 | 字段类型  | 字段说明
--- | --- | ---
md5 | string | 文件前 1M(不足 1M 取整个文件) 的 md5 值

### 2.4 返回结果

```json

```

### 2.5 返回参数

字段 | 字段类型  | 字段说明
--- | --- | ---

## 3. 分片上传

### 3.1 功能描述

前端一个一个上传分片数据, 后端保存分片数据信息

### 3.2 请求说明

> 请求说明: <br>
> 请求方式: POST<br>
> 请求PATH: `/upload`

### 3.3 请求参数

字段 | 字段类型  | 字段说明
--- | --- | ---
md5 | string | 文件前 1M(不足 1M 取整个文件) 的 md5 值

### 3.4 返回结果

```json

```

### 3.5 返回参数

字段 | 字段类型  | 字段说明
--- | --- | ---

## 4. 完成上传-把所有分片拼接成一个文件

### 4.1 功能描述

所有分片上传完成后, 通知后端拼接所有文件为一个文件并存储到数据系统中

### 4.2 请求说明

> 请求说明: <br>
> 请求方式 POST<br>
> 请求PATH: `/upload/fileMerge`

### 4.3 请求参数

字段 | 字段类型  | 字段说明
--- | --- | ---
md5 | string | 文件前 1M(不足 1M 取整个文件) 的 md5 值

### 4.4 返回结果

```json

```

### 4.5 返回参数

字段 | 字段类型  | 字段说明
--- | --- | ---

## 5. 文件检查

### 5.1 功能描述

检查当前文件是否已经上传过

### 5.2 请求说明

> 请求说明: <br>
> 请求方式 POST<br>
> 请求PATH: `/upload/file-check`

### 5.3 请求参数

字段 | 字段类型  | 字段说明
--- | --- | ---
md5 | string | 文件前 1M(不足 1M 取整个文件) 的 md5 值

### 5.4 返回结果

```json

```

### 5.5 返回参数

字段 | 字段类型  | 字段说明
--- | --- | ---

## 6. 文件检查

### 6.1 功能描述

检查当前文件是否已经上传过

### 6.2 请求说明

> 请求说明: <br>
> 请求方式 POST<br>
> 请求PATH: `/upload/file-check`

### 6.3 请求参数

字段 | 字段类型  | 字段说明
--- | --- | ---
md5 | string | 文件前 1M(不足 1M 取整个文件) 的 md5 值

### 6.4 返回结果

```json

```

### 6.5 返回参数

字段 | 字段类型  | 字段说明
--- | --- | ---

## 7. 文件检查

### 7.1 功能描述

检查当前文件是否已经上传过

### 7.2 请求说明

> 请求说明: <br>
> 请求方式 POST<br>
> 请求PATH: `/upload/file-check`

### 7.3 请求参数

字段 | 字段类型  | 字段说明
--- | --- | ---
md5 | string | 文件前 1M(不足 1M 取整个文件) 的 md5 值

### 7.4 返回结果

```json

```

### 7.5 返回参数

字段 | 字段类型  | 字段说明
--- | --- | ---

## 8. 文件检查

### 8.1 功能描述

检查当前文件是否已经上传过

### 8.2 请求说明

> 请求说明: <br>
> 请求方式 POST<br>
> 请求PATH: `/upload/file-check`

### 8.3 请求参数

字段 | 字段类型  | 字段说明
--- | --- | ---
md5 | string | 文件前 1M(不足 1M 取整个文件) 的 md5 值

### 8.4 返回结果

```json

```

### 8.5 返回参数

字段 | 字段类型  | 字段说明
--- | --- | ---
